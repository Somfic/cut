[package]
name = "cut-render"
version = "0.1.0"
edition = "2024"

[dependencies]
# Must match the front end's wgpu, so the `Device` and `Queue` handed in are
# the same types this was built for.
wgpu = "27"
