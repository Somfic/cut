[workspace]
members = ["crates/app", "crates/engine"]
default-members = ["crates/app"]
resolver = "3"

# Shared across both crates so a version bump happens in one place.
[workspace.dependencies]
anyhow = "1.0.104"
futures = "0.3"
futures-timer = "3.0.4"
